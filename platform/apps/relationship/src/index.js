/**
 * Relationship engine worker (MVP placeholder).
 * Edge inference currently runs inside the API discovery pipeline.
 * This process is reserved for async graph projection / decay jobs.
 */
console.log("AgentRadar relationship worker idle — projection handled by API pipeline in MVP.");
setInterval(() => {}, 60 * 60 * 1000);
